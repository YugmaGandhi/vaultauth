meta {
  name: Update User
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/admin/users/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "isVerified": true
  }
}
