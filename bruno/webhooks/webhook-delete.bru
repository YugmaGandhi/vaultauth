meta {
  name: Delete Webhook Endpoint
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/orgs/{{orgId}}/webhooks/{{webhookEndpointId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
